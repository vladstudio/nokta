---
url: https://mediabunny.dev/api/FilePathSourceOptions
title: FilePathSourceOptions | Mediabunny
---

# FilePathSourceOptions | Mediabunny

# FilePathSourceOptions

Options for `FilePathSource`.

```
type FilePathSourceOptions = {
	maxCacheSize?: number;
};
```

## Properties

### `maxCacheSize`

```
maxCacheSize?: number;
```

The maximum number of bytes the cache is allowed to hold in memory. Defaults to 8 MiB.